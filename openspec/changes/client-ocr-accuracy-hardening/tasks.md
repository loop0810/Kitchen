## 1. 权威文档与质量基线

- [x] 1.1 更新 `docs/product/PRODUCT_REQUIREMENTS.md`、`docs/product/MVP_REQUIREMENTS.md`、`docs/client/services/IMPORT_PIPELINE_SERVICE.md`、`docs/client/services/MEDIA_ASSET_SERVICE.md` 和 `docs/decisions/DECISION_LOG.md`，明确 OCR 分层质量指标、条件式裁剪/增强、Android V1 验收以及不默认转换繁简字形的边界。
- [x] 1.2 定义图片级 benchmark manifest、标准答案 schema、许可/隐私说明和版本控制外私有样本目录规则，确保只使用自制、脱敏或已授权仓库样本且不记录绝对路径。
- [x] 1.3 实现可重复的质量 runner，分别计算字符错误率、有效行召回、无关内容残留、菜谱字段准确率或列表 F1、校对编辑距离、单页耗时和峰值内存，并按场景输出匿名报告。
- [x] 1.4 增加覆盖清晰、模糊、小字号、低对比度、旋转、多栏、无关界面、简体、繁体和合法混排的最小图片 benchmark 集及人工标准答案，同时保留现有 OCR replay fixture 的布局回归职责。
- [ ] 1.5 在 Android 真机上记录未优化基线，为各场景写入首版质量与性能预算，并验证同版本重复执行得到可比较结果。（已采集并导出两轮基线；预算未全部通过，暂不勾选。）

## 2. OCR 质量领域模型与兼容持久化

- [x] 2.1 按客户端命名规范新增图片质量报告、问题码、推荐动作、OCR 文字质量、平台质量元数据、预处理来源和纠错建议领域模型，为每个模型属性补齐中文文档注释并通过 package 公共入口导出必要 API。
- [x] 2.2 扩展 OCR 行、页面和媒体引用的版本化 JSON 映射，明确 `null` 元数据与真实零值的区别，并用旧任务 fixture 验证缺少新字段时默认 `unknown` 且无需强制重识别。
- [x] 2.3 扩展 Import Repository 命令以原子保存质量报告、所选 OCR 候选、处理 profile/version、建议采用或拒绝状态和可撤销校对修订，并继续校验内容修订号与 processing generation。
- [x] 2.4 为新持久化语义增加数据库往返、旧数据读取、用户编辑保护、过期 generation 拒绝和导出恢复兼容测试。

## 3. 图片诊断、方向归一与条件式增强

- [x] 3.1 定义 OCR 输入准备端口和受控派生文件生命周期；若需新增图像处理依赖，记录现有能力不足、许可证、包体/性能影响、离线边界和失败回退后再锁定依赖。
- [x] 3.2 实现 EXIF 与用户旋转的物理方向归一，使 OCR 输入像素、宽高、预览方向和左上原点归一化坐标一致，并增加 0/90/180/270 度证据定位测试。
- [x] 3.3 实现不依赖文字语义的像素预检，输出方向、模糊、文字尺寸候选和低对比度等离散问题码；无法可靠判断时返回未知而非伪造正常结论。
- [x] 3.4 实现版本化的保守增强 profile，仅在预检命中时生成至多一个尺寸调整、灰度/对比度和轻度锐化候选，禁止覆盖受控原图。
- [x] 3.5 实现派生图写入失败回退、未选候选清理、内容修订失效和任务删除后的孤立文件清理测试。

## 4. 平台 OCR 元数据与候选选择

- [x] 4.1 扩展 Android ML Kit 通道映射，返回引擎实际提供的行置信度、角度和识别语言，保留真实零值并为缺失值返回空，同时验证打包模型无网络可用。
- [x] 4.2 扩展 iOS Vision 映射和回放，验证识别语言优先级、置信度、方向及坐标契约，不把配置语言伪装成逐行检测语言。
- [x] 4.3 在流水线中先识别原图候选，仅在质量策略命中时串行识别一个增强候选，并在每次原生调用和写回前继续执行取消、任务存在性和 generation 检查。
- [x] 4.4 实现候选质量比较策略，综合有效正文覆盖、可用置信度、乱码/孤立字符风险、菜谱关键区覆盖和新增噪声，在不确定或增强回退时选择原图。
- [x] 4.5 增加原图胜出、增强图胜出、增强失败、全页无文字、多页部分失败、处理中修改媒体和最多两次 OCR 调用上限的流水线测试。

## 5. 文字质量、繁简等价匹配与建议式纠错

- [x] 5.1 实现 OCR 文本后检，结合行坐标、元数据、有效正文和平台噪声生成页面/文档级 `usable`、`needsAttention`、`insufficient` 结论及风险行证据。
- [x] 5.2 将 OCR 文字质量与 `RecipeDraftQuality` 分开传递和展示数据，增加“结构完整但文字需校对”与“文字清晰但来源字段缺失”的领域测试。
- [x] 5.3 实现只用于内部结构化匹配的繁简等价键，补齐繁体分区标题和菜谱词识别测试，确保机器 OCR 原文、字段证据和默认草稿值不被匹配键改写。
- [x] 5.4 实现数字、单位、菜谱词和疑似字形误识别的保守建议生成器，建议包含原文、候选、原因和证据位置，合法繁体、地区用字及合法混排不得被自动判错。
- [x] 5.5 实现单项建议采用/拒绝与整段繁简转换预览、确认、撤销语义，把结果写入用户校对层，并验证重新 OCR、重新结构化不会覆盖已编辑、已拒绝或已确认内容。

## 6. 条件式用户引导与确认体验

- [x] 6.1 在图片管理区展示每页最相关的质量问题和下一步动作；清晰图片保持零打断，风险图片提供继续识别、裁剪、旋转、替换或手动整理入口。
- [x] 6.2 完善裁剪前说明，提示只保留一道菜的菜名、食材和完整步骤并去除评论、作者、推荐和系统界面内容，同时保留自由比例、取消和原图回看能力。
- [x] 6.3 在确认页分别展示菜谱结构问题和 OCR 文字质量问题，将低质量关键行定位到对应图片与原文证据，不向用户展示不可解释的置信百分比。
- [x] 6.4 增加纠错建议交互和用户主动繁简转换的差异预览、确认与撤销入口，默认保留机器识别字形，并明确转换属于用户编辑。
- [x] 6.5 为清晰图无打断、无关内容裁剪引导、严重模糊替换建议、繁体默认保留、建议拒绝、转换撤销、系统文字缩放、中文语义标签和 Tooltip 增加 Widget 测试。

## 7. 回归验证与验收

- [x] 7.1 运行并修复 Import Domain、Data、Feature 的单元、数据库、回放和 Widget 测试，确认现有分页失败、增量重试、原图保留和用户编辑保护行为不回退。
- [ ] 7.2 使用公开 benchmark 和版本控制外私有集生成优化后报告，验证目标场景改善且所有已配置质量/性能预算通过，并人工复核样本差异不是标准答案或规则偏差。（公开集已生成真实 Android 报告；部分场景预算未通过，私有集未提供。）
- [ ] 7.3 在 Android 真机无网络、未登录条件下验收相册与分享导入、旋转、条件式增强、裁剪、噪声提示、简繁保留、建议纠错、校对确认和待完善保存全链路。（已完成离线 OCR/旋转/增强候选采集；完整 UI 导入—确认—保存仍待人工操作。）
- [x] 7.4 运行 iOS Vision 平台契约与 OCR replay 回归，确认方向、坐标、置信度和默认字形语义一致，并记录 iOS 真机质量验收延期边界。
- [x] 7.5 执行 Dart 格式检查、客户端静态分析、全部 Flutter 测试、package 测试和导航边界检查，修复本 change 引入的问题。
- [x] 7.6 根据最终实现和实测预算回写相关客户端服务文档、MVP 当前状态与决策影响，记录新增依赖、派生文件清理、失败回退和 benchmark 复现命令。
