## 1. 对齐产品范围与权威文档

- [x] 1.1 更新 `PRODUCT_REQUIREMENTS.md`、`MVP_REQUIREMENTS.md` 和 `RECIPE_REQUIREMENTS.md`，将核心闭环改为菜谱导入、整理、管理与完整查阅，并移除逐步烹饪、烹饪记录、CookSession 及烹饪历史衍生需求
- [x] 1.2 更新 `IMPORT_PIPELINE_SERVICE.md`，补充媒体管理、分页失败、增量重处理、用户校对分层、字段保护和可解释确认的服务契约
- [x] 1.3 更新 `services/README.md` 和根 `AGENTS.md` 的产品约束，移除烹饪运行时服务入口与烹饪模式约束，并删除已废弃的 `COOKING_RUNTIME_SERVICE.md`
- [x] 1.4 在 `DECISION_LOG.md` 新增替代决策，明确永久聚焦菜谱与 OCR 确认闭环，并标注被替代的烹饪相关决策影响
- [x] 1.5 更新 `README.md`、MVP 当前实现状态、推荐顺序和验收清单，确保不再承诺“做过”、烹饪模式、计时或烹饪记录

## 2. 移除 Recipe 层烹饪运行时语义

- [x] 2.1 从 Recipe Domain 的公开实体、查询和排序契约中移除烹饪次数、最近烹饪时间、“做过”筛选、“最近做过”和“最常制作”，同时保留菜谱内容所需的时间、火力和步骤字段
- [x] 2.2 在 Recipe Data 中停止映射和查询烹饪统计，为旧筛选或排序偏好增加回退到“全部”或“最近更新”的兼容读取，并保留 SQLite 遗留列避免破坏性表重建
- [x] 2.3 从菜谱库筛选排序 UI、详情页和导航契约中移除烹饪历史入口及“开始烹饪”按钮，使完整详情成为查阅终点
- [x] 2.4 更新 Recipe Domain、Data、Library 和根 App 测试，验证旧数据库可读取、遗留统计不影响结果且界面不再出现烹饪运行时操作

## 3. 扩展 Import Domain 的可恢复模型

- [x] 3.1 按命名规范扩展媒体模型，为每页增加稳定 ID、原图与当前有效图引用、内容修订号、顺序、忽略状态以及 pending/processing/succeeded/failed OCR 状态和错误信息
- [x] 3.2 扩展 ImportTask 与草稿模型，分别表达机器 OCR 汇总、用户校对正文、补充说明、处理 generation、字段确认状态和冲突候选，并为每个新增模型属性补充中文文档注释
- [x] 3.3 为 ImportTaskRepository 增加追加、替换、重排、裁剪结果提交、旋转、忽略/恢复、分页重试、保存校对正文、保存补充说明和更新确认草稿等领域命令
- [x] 3.4 实现并测试保守草稿合并策略：自动字段可更新，用户编辑或确认字段不得覆盖，用户编辑过的食材或步骤列表按整体保护

## 4. 升级 Import Data 持久化与媒体生命周期

- [x] 4.1 升级 Import Drift schema，持久化 generation、机器 OCR、用户校对正文、补充说明和版本化确认草稿，并为新增列、迁移和事务补充中文注释
- [x] 4.2 更新媒体 JSON Mapper，兼容旧 `ocrCompleted` 数据并为缺失的修订号、分页状态和错误字段提供安全默认值
- [x] 4.3 实现媒体领域命令的原子持久化、受控文件写入与引用切换，确保失败时回滚数据库引用且不操作受控目录外文件
- [x] 4.4 扩展任务删除和孤立文件清理，使原图、派生裁剪图和被替换文件在无引用时安全清理
- [x] 4.5 增加数据库迁移、旧任务恢复、文件操作失败回滚、重排、忽略恢复和删除清理测试

## 5. 重构 OCR 流水线为增量且防过期回写

- [x] 5.1 让流水线基于不可变任务快照和 processing generation 执行，并在保存分页结果、汇总、草稿或错误前原子校验 generation
- [x] 5.2 将处理拆为分页 OCR、按当前顺序汇总、用户文字合并和草稿结构化阶段，使重排只重做汇总、单图变更只重做目标页、文字修改只重做结构化
- [x] 5.3 支持多图部分失败：保留成功页和证据，为失败页持久化可行动错误，并允许使用成功页继续生成部分草稿
- [x] 5.4 在重新处理时合并自动候选与确认草稿，保留用户字段并记录冲突提示，不让旧 generation 或新 OCR 静默覆盖用户修改
- [x] 5.5 增加并发换图、重排期间 OCR 完成、单页失败与重试、成功页复用、全页失败和应用恢复测试

## 6. 完成图片管理与失败降级界面

- [x] 6.1 在 `kitchen_import` 增加图片任务媒体工作区，展示可缩放缩略图、当前顺序、忽略状态、分页 OCR 状态和中文可访问性标签
- [x] 6.2 实现追加、单图替换、拖动重排、按 90 度旋转、忽略和恢复操作，所有图片操作完成后刷新任务并提供明确失败反馈
- [x] 6.3 为 Import Feature 接入已有能力无法满足的图片裁剪依赖，提供裁剪前后预览并将输出复制到应用受控目录后再提交媒体修订
- [x] 6.4 为部分失败和全部失败状态提供重试失败页、替换、裁剪/旋转、忽略、编辑文字、补充说明和转手动创建等下一步动作
- [x] 6.5 增加大文字、窄屏、语义标签、操作失败、全部图片忽略和多图重排 Widget 测试

## 7. 完成可解释草稿确认

- [x] 7.1 在 Import Feature 实现版本化确认页面，按问题摘要、基础信息、食材、准备工作、步骤、分类标签和来源分区展示
- [x] 7.2 为自动字段展示来源、离散可信等级和证据入口，支持查看对应页缩略图、OCR 摘录或文本行，并对无证据字段保持空值或待补充提示
- [x] 7.3 支持编辑所有草稿字段、调整食材与步骤顺序、确认字段、查看冲突候选，并在每次操作后自动持久化确认进度
- [x] 7.4 更新根 App 协调页，将最终确认的 Import Draft 映射为 Recipe 输入并通过现有 UseCase 幂等保存，不让 Import Feature 直接依赖 Recipe Feature
- [x] 7.5 支持退出后继续确认和保存缺少食材或步骤的待完善菜谱，移除“保存并开始烹饪”相关文案和行为
- [x] 7.6 增加字段证据、低可信提示、用户编辑保护、排序恢复、冲突候选、暂存恢复和重复确认幂等 Widget/集成测试

## 8. 回归验证与真实设备验收

- [x] 8.1 扩充真实多平台截图 OCR 回放集，覆盖模糊图、横竖图、多栏、拼接图、重复页边内容、部分失败和用户校对场景
- [x] 8.2 在无网络且不调用 AI 的条件下完成 Android ML Kit 真机 OCR 验收，并以自动化回归覆盖图片管理、部分失败、校对、确认和保存待完善菜谱；iOS 真机验收按 Android-only V1 策略延期且不阻塞本 change
- [x] 8.3 运行 `dart format --output=none --set-exit-if-changed .`、`flutter analyze`、`flutter test` 和 `flutter test packages/*/test`
- [x] 8.4 根据自动化与 Android 真机验收结果更新 MVP 验收勾选和当前实现状态，记录 iOS 延期，并确认仓库不再存在面向用户的烹饪运行时承诺或入口

## 9. 个性化食谱配置与时间选择器

- [x] 9.1 在 Recipe Domain/Data 增加分类、标签、难度配置实体、Repository、Drift 缓存、pending 同步和可注入远端网关，并在应用启动执行缓存优先同步
- [x] 9.2 在“我的”增加“个性化食谱”入口及分类、标签、难度的新增、重命名和删除界面，修改同时写入缓存并触发远端同步
- [x] 9.3 让导入确认和菜谱编辑统一订阅个性化配置缓存，删除各自硬编码选项及确认页管理入口
- [x] 9.4 将准备时间和制作时间改为小时、分钟双列选择器，分钟循环滚动、总时长限制两小时，并增加 Widget 与数据同步测试
