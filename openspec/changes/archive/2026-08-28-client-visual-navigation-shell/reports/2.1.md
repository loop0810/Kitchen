# client-visual-navigation-shell / 2.1: 使用品牌资源替换底部 Tab 默认图标

- Status: DONE
- Validation Level: Behavior
- Completed At: 2026-08-28 22:29 CST
- OpenSpec Task: 2.1

## Result

已将根导航壳的四组 Material 默认图标替换为本地 selected/unselected PNG 资源，并统一使用 `25×25` logical px 显示盒；导航分支切换逻辑保持不变。iOS 模拟器启动截图确认首页显示 selected/unselected 品牌图标。

## User-visible Behavior

- 用户能看到或使用：底部四个 Tab 显示品牌图标，当前 Tab 使用 selected 图形，其余 Tab 使用 unselected 图形。
- 系统责任：根 App `MainShell` 负责资源映射和显示尺寸，`StatefulNavigationShell` 继续负责导航状态。
- 快速重置或复现：恢复 `kitchen_notes_main_shell.dart` 和对应 Widget 测试即可；运行根 App Widget 测试并在 Android 竖屏切换四个 Tab。

## Changed Files

- `client/lib/src/navigation/kitchen_notes_main_shell.dart` — 使用 8 个品牌资源和固定图标显示盒。
- `client/test/kitchen_notes_widget_test.dart` — 验证 8 个资源路径和 `25×25` 尺寸。

## Acceptance Criteria

- [x] 当前 Tab 显示 selected 资源，其余 Tab 显示 unselected 资源。
- [x] selected 与 unselected 切换时图标尺寸和位置不发生布局跳动。
- [x] 现有导航栈恢复及重复点击行为验证通过。

## Validation

- 编译/静态/结构检查：PASS — `dart format --output=none --set-exit-if-changed .`；`./tool/kitchen_flutter.sh analyze`；`git diff --check`；`openspec validate client-visual-navigation-shell --strict --no-interactive`。
- 运行时/手动/编辑器验证：PARTIAL — iOS 模拟器 `iPhone 17` 成功启动，截图 `/private/tmp/kitchen-visual-task-2-1.png` 确认首页底部导航显示正确；`flutter devices` 未发现 Android 模拟器或实体设备，因此 Android 手动验证 NOT RUN。
- 自动化测试：PASS — `./tool/kitchen_flutter.sh test`，12 个客户端测试全部通过；其中 `test/kitchen_notes_widget_test.dart` 覆盖 8 个资源路径、`25×25` 图标尺寸、首页/菜谱库选中状态和重复点击当前 Tab。
- 重置/复现结果：从当前工程运行 `flutter run -d AA000123-7FA2-4B41-8D68-01A04F5126FE --debug --no-pub`；Widget 测试可重复验证资源映射和导航状态。

## Deviations

- 无代码范围偏差。初次测试曾因缺少 Material import、错误假设 NavigationBar 同时挂载 8 个 Image，以及测试装配未提供 ImportDependencies 而失败；已分别修正为补充 import、从 NavigationBar destinations 检查完整资源配置，并仅在已装配的首页/菜谱库分支执行交互验证。

## Remaining Issues

- Android 设备验证待 Task 4.1 在可用 Android 模拟器或实体设备上补充；不影响本 Task 的自动化验收结果。

## Next Task Context

Task 3.1 可继续调整主题层的顶部导航栏、底部导航栏表面和 App 背景；当前导航壳已提供稳定的图片资源和尺寸契约。
