# client-visual-navigation-shell / 1.1: 统一底部 Tab 图片资源命名并接入 Flutter 资源声明

- Status: DONE
- Validation Level: Structural
- Completed At: 2026-08-28 22:16 CST
- OpenSpec Task: 1.1

## Result

已将底部 Tab 的 8 个 PNG 资源统一为 `tab_<destination>_<selected|unselected>.png`，移除名称中的 `@3x`，并把资源声明接入根 Flutter 工程。

## User-visible Behavior

- 用户能看到或使用：本 Task 完成后，底部导航资源具备稳定名称并可被后续导航壳 Task 使用。
- 系统责任：根 Flutter 工程资源打包配置和客户端导航图片资源目录。
- 快速重置或复现：恢复本 Task 的资源重命名和 `client/pubspec.yaml` 资源声明即可；使用 `file client/assets/images/*.png` 和资源引用搜索复核。

## Changed Files

- `client/assets/images/` 下 8 个导航 PNG — 完成统一命名，保留 `75×75` 画布。
- `client/pubspec.yaml` — 显式声明 8 个导航图片资源。

## Acceptance Criteria

- [x] 8 个最终文件均符合统一命名，且名称不含 `@3x`。
- [x] 8 个 PNG 均为 `75×75`，Flutter 资源声明可被工程解析。
- [x] `rg` 搜索不到旧导航资源名的生产引用。

## Validation

- 编译/静态/结构检查：PASS — `flutter pub get`；`./tool/kitchen_flutter.sh analyze`；`dart format --output=none --set-exit-if-changed .`；`git diff --check`；`openspec validate client-visual-navigation-shell --strict --no-interactive`。
- 运行时/手动/编辑器验证：NOT RUN，本 Task 不涉及运行时导航。
- 自动化测试：NOT RUN，本 Task 仅修改资源名和 Flutter asset 声明；后续导航壳 Task 负责 Widget 行为验证。
- 重置/复现结果：`find client/assets/images -name '*.png' -exec file {} +` 可确认 8 个文件均为 `75×75`；资源旧名和 `@3x` 搜索无匹配。

## Deviations

- 无；工作区中已有的 `.DS_Store` 未纳入 Flutter 资源声明。

## Remaining Issues

- 资源目录中仍存在 `.DS_Store`，本 Task 不将其声明为 Flutter 资源。

## Next Task Context

Task 2.1 可使用以下最终资源名替换根导航壳中的 Material 默认图标：`tab_home_*`、`tab_recipe_library_*`、`tab_import_inbox_*`、`tab_profile_*`。
