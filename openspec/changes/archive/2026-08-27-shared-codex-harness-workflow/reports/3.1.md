# shared-codex-harness-workflow / 3.1: 走读并验证 Harness 与现有仓库现场的兼容性

- Status: DONE
- Validation Level: Structural
- Completed At: 2026-08-19 16:24，Asia/Shanghai
- OpenSpec Task: 3.1

## Result

完成 Harness 文档、根/端侧入口、OpenSpec 配置和本 Change 的一致性走读。现有客户端未提交实现、学习记录和其他工作区现场均保留；本 Change 只新增/修改规范与导航文件。

## Observable Behavior

- 用户/客户端能观察到：后续 Codex Task 可从根或端侧入口进入统一 Harness。
- 服务端/共享语义能观察到：服务端和共享任务有独立的验证/读取升级路径。
- 失败、恢复和幂等：未执行应用构建或集成验证，已明确写为 `NOT RUN`，不伪造通过。
- Reset / 复现：运行本 Report 中的 OpenSpec、链接、stale path 和 Git 命令。

## Changed Files

- `docs/Harness/` — 全部 Harness 规则与模板。
- `AGENTS.md`、`README.md`、`client/AGENTS.md`、`client/README.md`、`server/AGENTS.md`、`server/README.md` — 根/端侧入口。
- `docs/README.md`、`docs/client/README.md`、`docs/client/workflow/CODEX_WORKFLOW.md`、`docs/server/README.md`、`docs/learning/server/README.md` — 文档导航。
- `openspec/config.yaml`、当前 Change artifacts 和 reports — OpenSpec 规则与证据。

## Acceptance Criteria

- [x] `git status --short` 中已有文件仍未被本 change 改写或归类为本 change 成果。
- [x] `openspec status --change shared-codex-harness-workflow` 显示 proposal/design/tasks 完整且 specs 明确 skipped。
- [x] OpenSpec validate、Markdown/相对链接检查和 `git diff --check` 通过。
- [x] Report 和最终说明记录未运行的应用验证及后续采用方式。

## Validation

- 文档/OpenSpec：PASS；`openspec validate --strict shared-codex-harness-workflow` 通过。
- Markdown/链接：PASS；受影响入口共 22 个文件，相对链接和目录链接存在性检查通过。
- Stale 路径：PASS；未发现 `client/openspec`、旧示例路径或工作流中的 `git init`。
- 客户端格式/分析/测试：NOT RUN；本 Change 不修改客户端生产代码。
- Android/iOS/模拟器/设备：NOT RUN；不属于本 Change Scope。
- 服务端 lock/format/type/test：NOT RUN；不修改服务端生产代码。
- PostgreSQL/集成验证：NOT RUN。
- 共享 contract/跨端一致性：NOT RUN；不改变共享行为。
- Git 现场/checkpoint：PASS；`git diff --check` 通过，未提交当前工作区。

## Deviations

- 第一次宽范围链接扫描包含构建缓存第三方文档并将目录链接判为错误；已收窄为受影响规范入口并按目录存在性复核，最终检查通过。

## Remaining Issues

- 当前仓库原有客户端实现改动仍未提交，Harness 已规定不得自动处理；后续应由对应 Change/Task 继续验证和归属。
- 本 Change 未运行 Flutter 构建、Android 设备验收、服务端测试或 PostgreSQL 集成测试；这些不属于工作流文档变更的完成条件。

## Next Task Context

从本 Change 之后创建的新 Change 采用 Harness Task/Report 模板；既有活动 Change 不批量重写，只有自然修改相关任务时补充字段。
