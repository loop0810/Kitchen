> 当前验收优先以 Android 为主。由于 Personal Team 不支持 iOS 的 App Groups、Share Extension
> 和 Sign in with Apple，相关 iOS 真机任务暂缓；实现代码保留，待开发者团队可用后恢复验收。

## 1. iOS 扩展工程与共享容器

- [x] 1.1 创建 iOS Share Extension target、scheme、Info.plist 和扩展入口，并保持 Debug/Profile/Release 配置一致
- [x] 1.2 配置主 App 与扩展共同使用的 App Group capability，使用可注入的项目配置承载标识符，不提交环境绝对路径或签名材料
- [x] 1.3 声明文字、HTTPS URL 和常见图片 UTI 的接收范围，配置扩展显示名称、图标和中文系统分享文案
- [ ] 1.4 在真实 iOS 设备上验证主 App、扩展和 App Group 的签名/安装关系

## 2. 扩展侧暂存与清单

- [x] 2.1 实现版本化 manifest 模型，包含 shareId、创建时间、来源元数据、内容类型、顺序、文件大小和摘要
- [x] 2.2 实现 Share Extension 对文字、HTTPS 链接、单图和多图的读取与白名单校验
- [x] 2.3 将文字和图片流式复制到 App Group 受控暂存目录，避免只保存短期 NSItemProvider 或系统文件引用
- [x] 2.4 采用临时文件加原子替换写入 manifest，并用完成状态区分复制中的分享和可消费分享
- [x] 2.5 覆盖不支持类型、读取失败、部分媒体失败、超大载荷和暂存空间不足的可恢复错误状态

## 3. 主 App 接管与导入任务

- [x] 3.1 在主 App 启动和恢复前台流程中扫描待处理 manifest，并将 iOS 载荷转换为现有 Import Repository 输入
- [x] 3.2 校验 manifest 版本、shareId、文件摘要、文件数量、受控路径和内容类型后再创建 ImportTask
- [x] 3.3 将图片复制到现有应用受控媒体目录，并在任务持久化事务中保存原始文字、来源和媒体引用
- [x] 3.4 接管成功后将分享任务展示在导入箱，后续 OCR、草稿确认和保存复用现有流程
- [x] 3.5 接管失败时保留 manifest 与暂存内容，向导入箱或可恢复状态提供重试/清理入口

## 4. 幂等、清理与恢复

- [x] 4.1 以 shareId 建立本地接管记录或等价幂等判断，保证重复扫描不会创建重复 ImportTask
- [x] 4.2 处理任务创建中断、主 App 被终止和重复前台通知，验证下次启动可以继续接管
- [x] 4.3 仅在任务和受控媒体引用成功持久化后清理 App Group 暂存文件与 manifest
- [x] 4.4 增加清理失败的待重试状态，确保清理失败不删除已创建任务或受控媒体
- [x] 4.5 增加过期暂存清理策略，区分已接管、失败待重试和用户明确删除的内容

## 5. 测试与真实设备验收

- [ ] 5.1 为 manifest 编解码、原子写入、摘要校验、路径约束和重复消费增加单元测试
- [ ] 5.2 为主 App 接管流程增加文字、HTTPS 链接、单图、多图、来源元数据和顺序保持测试
- [ ] 5.3 增加损坏 manifest、缺失文件、部分复制失败、任务写入失败和清理失败测试
- [ ] 5.4 在主 App 前台、后台、未启动和被系统终止后重新启动的 iOS 设备场景验收分享接管
- [ ] 5.5 从 Safari、相册和至少一个第三方内容应用分别验证文字、链接、单图和多图分享
- [ ] 5.6 运行受影响 Flutter package 测试、iOS 构建和真实设备验收，更新客户端服务文档与 MVP 实现状态
