## 1. V1 范围与权威文档

- [x] 1.1 将 Android-only、无需登录、离线核心闭环写入 `docs/product/MVP_REQUIREMENTS.md` 的 V1 范围与非范围。
- [x] 1.2 更新客户端 README 和决策日志，明确 iOS、账号、服务端、备份和 AI 不属于 V1 发布前置条件。
- [x] 1.3 盘点现有 OpenSpec changes，标注 `refocus-mvp-recipe-ocr` 为 V1 主线，其余外围 change 为延期队列。

## 2. Android 核心导入闭环

- [ ] 2.1 在 Android 真机验证系统分享文字、链接、单图和多图都能进入持久化导入箱。
- [x] 2.2 验证原始图片或文字先持久化，再进入 OCR、提取和本地结构化处理。
- [x] 2.3 完成多图重排、替换、裁剪、旋转、忽略、恢复和逐页失败重试的核心路径。
- [x] 2.4 验证 OCR 失败或结构化失败时，用户可以编辑文字、手动补充或转为手动创建。
- [x] 2.5 验证应用中断后导入任务、原始材料、成功 OCR 页面和用户校对内容可恢复。

## 3. 草稿确认与菜谱保存

- [x] 3.1 验证 OCR 草稿展示字段来源、风险提示和可编辑状态。
- [x] 3.2 验证用户修改后的菜名、食材、步骤、分类、标签、笔记和来源不会被重新处理覆盖。
- [x] 3.3 验证草稿可以暂存、退出后继续确认，并可保存为待完善菜谱。
- [x] 3.4 验证重复确认和保存不会创建重复菜谱。

## 4. 本地菜谱使用闭环

- [x] 4.1 验证手动创建和完整编辑菜谱可离线完成。
- [x] 4.2 验证首页和菜谱库可以按菜名、食材和标签搜索本地菜谱。
- [x] 4.3 验证收藏、待完善筛选、详情查看、完整步骤展示和再次编辑可用。
- [x] 4.4 验证 V1 不出现登录阻塞、烹饪运行时、AI 调用或网络依赖文案。

## 5. V1 发布验收

- [ ] 5.1 补充 Android 真机核心流程验收记录，覆盖无网络、未登录和应用中断恢复。
- [ ] 5.2 扩充代表性截图 OCR 回放集，覆盖清晰图、模糊图、横竖图、多图和部分失败。
- [ ] 5.3 运行 `dart format --output=none --set-exit-if-changed .`、`flutter analyze`、`flutter test` 和 `flutter test packages/*/test`。
- [ ] 5.4 根据真机和自动化结果更新 MVP 验收清单，只在所有 V1 核心项通过后进入发布准备。

## 6. 延期队列整理

- [x] 6.1 将 iOS Share Extension 标记为 V1 后恢复项，并记录 App Store 发布前的双端验收要求。
- [x] 6.2 将 Apple、微信、手机号登录及账号配置同步标记为 V1 后恢复项。
- [x] 6.3 将本地备份恢复、云端同步、菜谱集增强、复杂模板和付费 AI 标记为 V1 后恢复项。
