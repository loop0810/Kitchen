## 1. 权威契约与产品边界

- [ ] 1.1 新增认证契约，定义 `userId`、身份范围、会话、错误 envelope、幂等键和账号删除语义
- [ ] 1.2 更新产品/MVP 与决策日志，明确登录可选、首次认证自动建号且本地核心功能不依赖账号服务
- [ ] 1.3 更新个性化配置契约，规定远端网关通过正式会话获取当前账号且不得自行传入可伪造用户 ID

## 2. 服务端账号与身份

- [ ] 2.1 在服务端建立 Auth 模块端口以及 User、AuthIdentity、DeviceSession、RefreshTokenFamily 和幂等记录模型
- [ ] 2.2 编写账号、身份唯一范围、设备会话和删除状态迁移及 PostgreSQL 迁移测试
- [ ] 2.3 实现已验证身份断言的事务内 find-or-create，并覆盖并发首次登录和结果未知重试
- [ ] 2.4 实现近期重新认证后的身份绑定、解绑约束和身份占用冲突，禁止隐式账号合并

## 3. 会话与删除安全

- [ ] 3.1 实现短期访问凭证、仅存摘要的刷新凭证、轮换和 token family 重放撤销
- [ ] 3.2 实现退出当前设备、退出全部设备和设备会话列表，并覆盖撤销后的访问/刷新测试
- [ ] 3.3 实现账号删除状态机、全部会话撤销和可重试服务端数据清理边界
- [ ] 3.4 增加统一鉴权错误、账号枚举防护、请求幂等和安全审计日志测试

## 4. Flutter 会话基础

- [ ] 4.1 新增符合客户端组件边界的认证 domain/data 端口、会话状态和根 App 依赖装配
- [ ] 4.2 使用 Keychain/Keystore 类安全存储保存刷新凭证，并验证 Drift、偏好和系统普通备份不含凭证
- [ ] 4.3 接入匿名、认证中、已登录、刷新中和失效状态，确保应用启动会话恢复不阻塞本地首屏
- [ ] 4.4 在“我的”增加账号状态、退出当前设备、退出全部设备和账号删除入口
- [ ] 4.5 将账号删除与“同时清除本机数据”显示为两个独立确认项，不在本 change 实现菜谱云端同步

## 5. 跨端验证与文档

- [ ] 5.1 增加服务端 Auth 集成测试和 Flutter Repository/Widget 测试，覆盖离线、并发、重放、冲突和删除
- [ ] 5.2 运行服务端与 Flutter 对应格式化、静态分析和测试命令，并记录验证结果
- [ ] 5.3 更新服务端 Auth、客户端会话与安全存储文档，确保共享规范只在认证 contract 定义
- [ ] 5.4 创建本轮服务端学习记录或在 tasks 中记录学习豁免依据
