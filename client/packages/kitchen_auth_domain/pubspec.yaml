name: kitchen_auth_domain
description: Pure Dart authentication and session contracts for Kitchen Notes.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
